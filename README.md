# Befehlsbibliothek (Vue 3 + Vite + PWA)

Eine kleine App zur Sammlung von Befehlen (z. B. Bash, Docker, Git ...).

## Installation

```bash
# Install dependencies
npm install

# Start dev server
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview
